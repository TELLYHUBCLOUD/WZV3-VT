services:
  app:
    build: .
    command: bash start.sh
    restart: on-failure
    network_mode: "host"
    volumes:
      - ./torrents:/usr/src/app/torrents
      - ./config.py:/usr/src/app/config.py:ro
